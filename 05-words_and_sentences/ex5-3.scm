(include "../harvey/simply.scm")

(first 'mezzanine)
;; argument is a word
;; function will return the first character of the word: m

(first '(mezzanine))
;; argument is a sentence
;; function will return the first word(which also happens to be the only word): mezzanine
