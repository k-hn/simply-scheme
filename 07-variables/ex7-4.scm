(define (sum-square a b)
  (let ((+ *)
	(* +))
    (* (+ a a) (+ b b))))

;; the procedure works because the prodcudures `*` and `+` have been swapped
;; what would have otherwise been the product of second multiples of a and b
;; is instead now, the sum of the squares of a and b
